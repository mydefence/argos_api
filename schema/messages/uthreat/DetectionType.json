{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "DetectionType",
    "type": "string",
    "description": "The type of the system component sending the threat.",
    "enum": ["RF", "Radar", "Drone ID", "Demodulated", "Audio", "Vision"]
}
