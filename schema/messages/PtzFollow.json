{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "ptzFollow",
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "ptzFollowId": {
            "type": "string",
            "description": "ID of the PTZ follow instance"
        },
        "deviceId": {
            "type": "string",
            "description": "Device ID of the PTZ device used for following"
        },
        "followUthreatId": {
            "type": "string",
            "description": "uthreat ID of the threat that is followed. Present when following a threat"
        },
        "followDeviceId": {
            "type": "string",
            "description": "Device ID of device that is followed (e.g. GPS). Present when following a device"
        }
    },
    "required": ["ptzFollowId", "deviceId"]
}
